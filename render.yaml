services:
  - type: web
    name: careco-complete-care-price-watch
    runtime: python
    plan: free
    region: frankfurt
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn app:app --workers 1 --threads 4 --timeout 120
    autoDeploy: true
